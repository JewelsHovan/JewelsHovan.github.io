import React from 'react';
import './css/HeroSection.css'; // Create a separate CSS file for styling

const HeroSection = () => {
  return (
    <div className="hero">
      {/* Optionally, add a background image here */}
      <h1>Starting My Journey: Where Computer Science, Biology, and AI Converge</h1>
    </div>
  );
};

export default HeroSection;
